● InkOS 全特性最佳使用指南

  从安装到产出的全流程精细化操作说明，覆盖所有特性的使用场
  景和时机：

  ---
  🚩 第一阶段：安装与配置（首次使用时执行）

  使用时机：刚获取InkOS，还未做任何配置

  操作时机: 安装工具
  命令: npm i -g @actalk/inkos
  作用: 全局安装InkOS CLI
  关键参数说明: -
  ────────────────────────────────────────
  操作时机: （可选）OpenClaw生态安装
  命令: clawhub install inkos
  作用: 安装为OpenClaw Skill，可被Claude
  Code等Agent直接调用
  关键参数说明: -
  ────────────────────────────────────────
  操作时机: 配置全局API（推荐，只需配一次）
  命令: inkos config set-global --provider <provider>
    --base-url <url> --api-key <key> --model <model>
  作用: 配置全局LLM参数，所有项目共享
  关键参数说明: provider:
    openai/anthropic/custom（兼容OpenAI格式的中转站选custom
  ）base-url:
     API地址model: 模型名
  ────────────────────────────────────────
  操作时机: 验证配置正确性
  命令: inkos doctor
  作用: 自动诊断配置问题、测试API连通性、给出兼容性提示
  关键参数说明: 必跑命令，避免后续写的时候才发现配置错误
  ────────────────────────────────────────
  操作时机: （进阶）多模型路由配置
  命令: inkos config set-model <agent-name> <model>
    --provider <provider>
  作用: 给不同Agent分配不同模型，平衡质量与成本
  关键参数说明: 推荐配置：•
  写手(Writer)用高能力模型（Claude
    4.6/GPT-4o）•
    审计(Auditor)用低成本快模型（GPT-4o-mini/豆包）•
    雷达(Radar)用本地模型零成本
  ────────────────────────────────────────
  操作时机: 查看模型路由
  命令: inkos config show-models
  作用: 查看当前各Agent的模型分配情况
  关键参数说明: -

  ---
  🚩 第二阶段：项目初始化（开启新创作项目时执行）

  使用时机：要开始写一本/多本新书的时候

  操作时机: 初始化项目目录
  命令: inkos init [项目名]
  作用: 创建InkOS项目骨架，省略项目名则在当前目录初始化
  关键参数说明: 执行后会生成.env、inkos.json配置文件
  ────────────────────────────────────────
  操作时机: （可选）项目级配置
  命令: 编辑项目根目录.env
  作用:
  项目级配置会覆盖全局配置，适合不同项目用不同模型/key
  关键参数说明: -
  ────────────────────────────────────────
  操作时机: 创建新书
  命令: inkos book create --title <书名> --genre <题材>
  作用: 创建一本新书，生成基础设定骨架
  关键参数说明: --genre:
    支持xuanhuan(玄幻)/xianxia(仙侠)/urban(都市)/sci-fi(科
  幻)等，全量题材用inkos
     genre list查看--brief
    <设定文档路径>：传入你自己写的脑洞/世界观/人设文档，系
  统会基于此生成设定而非凭空创作--chapter-words
     <字数>：每章默认目标字数--target-chapters
    <数量>：全书总目标章节数
  ────────────────────────────────────────
  操作时机: 查看已有书籍
  命令: inkos book list
  作用: 列出当前项目下所有书籍
  关键参数说明: 只有一本书时后续命令可以省略书籍ID参数
  ────────────────────────────────────────
  操作时机: 修改书籍设置
  命令: inkos book update <书籍ID> --chapter-words <字数>
  作用: 修改单本书的配置
  关键参数说明: 可修改字数、总章节数、状态等

  ---
  🚩 第三阶段：创作前准备（正式写作前执行，一次性配置）

  使用时机：建书后，写第一章之前，根据你的需求选择执行

  适用场景: 已有小说文本要续写
  命令: inkos import chapters <书籍ID> --from <文本路径>
  作用: 导入已有章节，自动逆向生成所有真相文件（世界状态/角
  色矩阵/伏笔等），后续可直接write
    next续写
  关键参数说明: --split
    <分割规则>：自定义章节分割模式，默认按第X章分割--resume
  -from
     <章节号>：断点续导
  ────────────────────────────────────────
  适用场景: 要写同人作品
  命令: inkos fanfic init --from <原作路径> --mode <模式>
  作用:
  从原作素材创建同人书，内置同人专属审计规则，避免设定冲突
  关键参数说明: --mode:
    canon(正典延续)/au(架空世界)/ooc(性格重塑)/cp(CP向)
  ────────────────────────────────────────
  适用场景: 要模仿特定文风写作
  命令: 1. inkos style analyze <参考文本路径>2. inkos style

    import <分析结果路径> <书籍ID>
  作用: 第一步分析参考文本的文风指纹（句长分布、词频、节奏
  模式），第二步注入到指定书籍，后续所有章节自动采用该风格
  关键参数说明: -
  ────────────────────────────────────────
  适用场景: 调整长期创作方向
  命令: 编辑story/author_intent.md
  作用: 写入这本书的长期定位、核心卖点、禁止内容等，所有章
  节创作都会遵循这个意图
  关键参数说明: -
  ────────────────────────────────────────
  适用场景: 调整近期创作重点
  命令: 编辑story/current_focus.md
  作用:
  写入最近1-3章的重点方向，比如"接下来三章重点推进夺宝剧情"
  关键参数说明: -
  ────────────────────────────────────────
  适用场景: 预规划章节（无API也可跑）
  命令: inkos plan chapter <书籍ID> --context
    "本章重点写师徒矛盾"
  作用: 生成本章意图文件（chapter-XXXX.intent.md），包含本
  章必须保留/必须避免的内容
  关键参数说明: 不需要调用LLM，零成本，建议写作前必跑，确认
  你的指令被正确理解
  ────────────────────────────────────────
  适用场景: 预编译上下文（无API也可跑）
  命令: inkos compose chapter <书籍ID>
  作用: 生成上下文包、规则栈、执行轨迹，验证输入是否合法
  关键参数说明:
  不需要调用LLM，零成本，确认没问题再正式写作避免浪费token

  ---
  🚩 第四阶段：正式写作（日常创作阶段）

  使用时机：所有准备工作完成，开始写正文

  三种写作模式，根据你的需求选择：

  模式: 一键完整管线（新手/懒人推荐）
  适用场景: 想省事，写完自动审计修订，一步出成品
  命令: inkos write next <书籍ID>
  作用: 完整管线：规划→编排→写草稿→审计→自动修订→状态更新，
  直接生成可用章节
  关键参数说明: --count <N>：连续写N章--words
    <字数>：覆盖默认字数目标-q：静默模式，不输出详细日志
  ────────────────────────────────────────
  模式: 原子命令（精细控制/外部Agent调用）
  适用场景: 想自己把控每一步，或者和其他工具/Agent集成
  命令: 1. inkos draft <书籍ID>2. inkos audit <书籍ID>
    <章节号>3. inkos revise <书籍ID> <章节号>
  作用: 1. 只生成草稿，不做审计修订2.
    单独审计指定章节，输出33维度检查报告3.
    根据审计报告修订章节
  关键参数说明:
  所有命令都支持--json输出结构化数据，方便脚本/Agent调用
  ────────────────────────────────────────
  模式: 自然语言Agent模式
  适用场景: 不想记命令，用自然语言交互
  命令: inkos agent "帮我写一本都市修仙，主角是程序员"
  作用: 内置18个工具，LLM自动判断调用什么命令完成你的需求
  关键参数说明: 支持复杂指令："先扫描市场趋势，然后写一本符
  合当前爽点的玄幻书，重点写升级流"
  ────────────────────────────────────────
  模式: 后台自动写作
  适用场景: 不想守着，让它自动写，写完通知你
  命令: inkos up
  作用: 启动后台守护进程，自动连续写章，非关键问题全自动处
  理，关键问题暂停等人工审核
  关键参数说明: 支持Telegram/飞书/企业微信/Webhook通知推送
  ，日志写入inkos.log
  ────────────────────────────────────────
  模式: 重写已写章节
  适用场景: 对已有的某章不满意，要重写
  命令: inkos write rewrite <书籍ID> <章节号>
  作用:
  恢复到该章节写之前的状态快照，重新走完整管线生成新章节
  关键参数说明: --force：跳过确认直接重写
  ────────────────────────────────────────
  模式: 停止后台写作
  适用场景: 要终止自动写作
  命令: inkos down
  作用: 停止后台守护进程
  关键参数说明: -

  ---
  🚩 第五阶段：质量管控（章节写完后执行）

  使用时机：草稿生成后，发布前做质量检查和优化

  操作时机: 查看待审核章节
  命令: inkos review list <书籍ID>
  作用: 列出所有需要人工审核的章节草稿
  关键参数说明: -
  ────────────────────────────────────────
  操作时机: 批量通过审核
  命令: inkos review approve-all <书籍ID>
  作用: 批量通过所有待审核章节，正式纳入正典
  关键参数说明: -
  ────────────────────────────────────────
  操作时机: 单独审计章节
  命令: inkos audit <书籍ID> <章节号>
  作用: 输出33维度详细审计报告，包含连续性问题、AI痕迹、节
  奏问题等
  关键参数说明: -
  ────────────────────────────────────────
  操作时机: 修复审计问题
  命令: inkos revise <书籍ID> <章节号>
  作用: 自动修复审计发现的问题，关键问题标记给人工审核
  关键参数说明: --mode
    anti-detect：专门做AI去痕迹改写，降低被检测到的概率
  ────────────────────────────────────────
  操作时机: AI生成痕迹检测
  命令: inkos detect <书籍ID> <章节号>
  作用: 检测章节的AI生成痕迹
  关键参数说明:
  --all：检测所有章节--stats：输出全本AI痕迹统计
  ────────────────────────────────────────
  操作时机: 查看创作数据分析
  命令: inkos analytics <书籍ID>
  作用: 输出全本统计数据：审计通过率、高频问题、章节排名、t
  oken用量等
  关键参数说明: 建议写3-5章就跑一次，根据高频问题调整设定，
  避免后面重复出问题
  ────────────────────────────────────────
  操作时机: 查看项目状态
  命令: inkos status <书籍ID>
  作用: 查看书籍总进度、已写字数、活跃伏笔数、资源追踪项等
  全局状态
  关键参数说明: -

  ---
  🚩 第六阶段：导出与发布（全书写完/阶段性写完后执行）

  使用时机：要导出成文件发布/阅读的时候

  需求: 导出全本
  命令: inkos export <书籍ID>
  作用: 默认导出为txt格式
  关键参数说明: --format <格式>：支持txt/md/epub--output
    <路径>：指定导出文件路径--approved-only：只导出已经审核
  通过的章节
  ────────────────────────────────────────
  需求: 导出EPUB格式
  命令: inkos export <书籍ID> --format epub
  作用: 导出为EPUB格式，适合手机/Kindle阅读
  关键参数说明: -

  ---
  🚩 进阶特性使用场景

  需求: 想了解当前网文市场趋势，调整故事方向
  命令: inkos radar scan
  作用: 扫描主流平台的热门题材、爽点、读者偏好，生成趋势报
  告指导创作
  ────────────────────────────────────────
  需求: 要写番外/衍生作品，沿用正传设定
  命令: inkos import canon <番外书籍ID> --from <正传书籍ID>
  作用: 导入正传的所有设定到番外书，避免设定冲突
  ────────────────────────────────────────
  需求: 更新InkOS到最新版本
  命令: inkos update
  作用: 升级到最新版，获取新特性和bug修复
  ────────────────────────────────────────
  需求: 删除不需要的书籍
  命令: inkos book delete <书籍ID> --force
  作用: 删除指定书籍及所有数据

  ---
  📋 最佳实践工作流参考

  graph LR
  A[安装inkos] --> B[config set-global配置全局API]
  B --> C[inkos doctor验证配置]
  C --> D[inkos init初始化项目]
  D --> E[inkos book create建书]
  E --> F{有没有特殊需求?}
  F -->|要续写| G[inkos import chapters导入旧文本]
  F -->|要写同人| H[inkos fanfic init创建同人书]
  F -->|要仿写| I[inkos style analyze/import导入文风]
  F -->|无特殊需求| J[编辑author_intent.md写长期意图]
  G/H/I/J --> K[inkos plan chapter规划本章]
  K --> L[inkos compose chapter编译上下文]
  L --> M{规划是否符合预期?}
  M -->|是| N[inkos write next写正文]
  M -->|否| O[调整context/author_intent重跑plan]
  N --> P[inkos audit审计章节]
  P --> Q{质量是否达标?}
  Q -->|是| R[inkos review approve-all通过审核]
  Q -->|否| S[inkos revise修订]
  S --> P
  R --> T[写完全书后inkos export导出]

  按这个流程操作可以100%发挥InkOS的所有特性，从根源避免设定
  冲突、AI痕迹重、剧情崩等常见问题。
